apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-hermes
  namespace: ai-demo
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 15Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-hermes
  namespace: ai-demo
  labels: { app: llama-hermes }
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector:
    matchLabels: { app: llama-hermes }
  template:
    metadata:
      labels: { app: llama-hermes, model: hermes-4-14b }
    spec:
      initContainers:
        - name: fetch-model
          image: curlimages/curl:8.10.1
          command: ["sh", "-c"]
          args:
            - |
              set -eu
              MODEL=/models/Hermes-4-14B-Q4_K_M.gguf
              if [ -f "$MODEL" ]; then
                echo "Model already present: $(ls -lh $MODEL)"
                exit 0
              fi
              echo "Downloading Hermes-4-14B-Q4_K_M (~9 GB)..."
              curl -L --fail --retry 3 --retry-delay 5 \
                -o "$MODEL.partial" \
                https://huggingface.co/bartowski/NousResearch_Hermes-4-14B-GGUF/resolve/main/NousResearch_Hermes-4-14B-Q4_K_M.gguf
              mv "$MODEL.partial" "$MODEL"
          volumeMounts:
            - { name: models, mountPath: /models }
          resources:
            requests: { cpu: "200m", memory: "128Mi" }
            limits:   { cpu: "2",    memory: "512Mi" }
      containers:
        - name: llama-server
          image: ghcr.io/ggml-org/llama.cpp:server
          args:
            - "-m"
            - "/models/Hermes-4-14B-Q4_K_M.gguf"
            - "--host"
            - "0.0.0.0"
            - "--port"
            - "8080"
            - "-c"
            - "8192"
            - "-t"
            - "12"
            - "--n-gpu-layers"
            - "0"
            - "--metrics"
          ports:
            - { name: http, containerPort: 8080 }
          volumeMounts:
            - { name: models, mountPath: /models, readOnly: true }
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 30
          resources:
            requests: { cpu: "4",  memory: "12Gi" }
            limits:   { cpu: "12", memory: "20Gi" }
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: models-hermes
---
apiVersion: v1
kind: Service
metadata:
  name: llama-hermes
  namespace: ai-demo
spec:
  selector: { app: llama-hermes }
  ports:
    - { name: http, port: 8080, targetPort: 8080 }
